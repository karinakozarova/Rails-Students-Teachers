<%= form_with(model: karina, local: true) do |form| %>
  <% if karina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(karina.errors.count, "error") %> prohibited this karina from being saved:</h2>

      <ul>
      <% karina.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
